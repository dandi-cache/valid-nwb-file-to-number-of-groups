[project]
name = "valid-nwb-file-to-number-of-groups"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "datalad",
    "datalad-container",
    "h5py",
    "remfile",
    "s3fs",
    "zarr<3",
]
